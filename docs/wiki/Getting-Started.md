# Getting Started

## Repository purpose

eDB — Embedded Multi-Model Database

## First steps

1. Read the [README](https://github.com/embeddedos-org/eDB/blob/master/README.md) for the project's supported setup and usage path.
2. Clone the repository and enter its directory:

```bash
git clone https://github.com/embeddedos-org/eDB.git
cd eDB
```

3. Check the root project inputs below before installing dependencies or selecting a build tool.
4. Review [Development](Development) before changing code, and [Security](Security) before reporting a vulnerability.

## Root project inputs

- `Dockerfile`: Container build definition.
- `docker-compose.yml`: Docker Compose definition.
- `package-lock.json`: npm dependency lock.
- `package.json`: Node.js package manifest.
- `pyproject.toml`: Python project manifest.

## Scope note

The default branch inspected for this page was `master` at [`0457c0ace631`](https://github.com/embeddedos-org/eDB/commit/0457c0ace631cd540529a53f6c63c37fed467286). This page intentionally does not invent a universal build command when the repository's own documentation does not provide one.
